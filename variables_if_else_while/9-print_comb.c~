#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int number;
	int i;
	for (number = 48; number < 58; number++)
	{
		putchar(number);
		while (number != 57)
		{
			putchar(44);
			i++;
		}
	}
	putchar('\n');
	return (0);
}
